# Core dependencies
PyYAML>=6.0
pyod>=1.0
yara-python>=4.4.0
requests>=2.25
Flask>=2.0

# Optional: For better data handling (used in anomaly detection)
numpy>=1.21.0
pandas>=1.3.0

# For SOAR and automation
# Note: Shuffle runs separately via Docker, but you can extend with:
python-dotenv  # If using environment variables

# For dashboard development (if extending Grafana or building React)
# React dashboard would be separate; Flask serves JSON API
